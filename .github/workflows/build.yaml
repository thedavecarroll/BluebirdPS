name: platyPS

on:
  push:
#    branches:
#      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Install psake, BuildHelpers and PowerShellBuild
      shell: pwsh
      run: |
        Set-PSRepository PSGallery -InstallationPolicy Trusted
        Install-Module psake, BuildHelpers, PowerShellBuild
        Import-Module psake, BuildHelpers, PowerShellBuild
    - name: Build BlueBirdPS
      shell: pwsh
      run: |
        ./build.ps1